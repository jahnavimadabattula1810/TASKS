server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/multitask_management
    username: root
    password: M.Jahnavi@123
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect

logging:
  level:
    root: INFO
    com.task.notes_application: DEBUG
